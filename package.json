{
  "name": "gacosplit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo Tailwind CDN handles styling at runtime — no build step needed",
    "build:css": "echo Tailwind CDN handles styling at runtime — no build step needed"
  },
  "devDependencies": {}
}
